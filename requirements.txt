#  data handling 
numpy>=1.26
pandas>=2.1
scipy>=1.11

#  mixed-effects modeling + power simulation 
statsmodels>=0.14        # MixedLM for the LMM and power_analysis_simulation.ipynb

#  figures 
matplotlib>=3.8
seaborn>=0.13

#  .env file integration 
python-dotenv>=1.0       # loads .env so OPENAI_API_KEY is available

#  notebook kernel + display (DataSpell runs notebooks against this env) 
ipykernel>=6.25
ipython>=8.0             # IPython.display.display() in the generator

#  stimulus generation (survey_generator.ipynb) 
openai>=1.0              # OpenAI API client for LLM stimulus generation

#  reading the Qualtrics export 
openpyxl>=3.1            # xlsx export
pyreadstat>=1.2          # SPSS .sav export (optional; remove if unused)